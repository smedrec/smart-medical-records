{
	"extends": "@repo/typescript-config/lib.json",
	"compilerOptions": {
		"noEmit": false,
		"outDir": "./dist",
		"declaration": true,
		"declarationMap": true,
		"types": ["node"] // Removed @cloudflare types
		// We inherit module, target, moduleResolution ('bundler'), lib, strict, etc., from the base.
	},
	"include": ["src/**/*"],
	"exclude": ["node_modules", "dist", "eslint.config.ts", "**/*.test.ts", "**/*.spec.ts"]
}
