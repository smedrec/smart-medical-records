{
	"extends": "@repo/typescript-config/base.json",
	"compilerOptions": {
		"noEmit": false,
		"outDir": "./dist"
	},
	"include": ["src/**/*.ts", "keys/**/*.key"],
	"exclude": [
		"node_modules",
		"dist",
		"**/*.test.ts", // Assuming tests shouldn't be part of the dist output
		"**/*.spec.ts"
	] // Assuming tests shouldn't be part of the dist output]
}
